spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password:
    driver-class-name: org.h2.Driver
    generate-unique-name: false
    name: vet-clinic-test
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update